---
const { title } = Astro.props;
---

<footer><p>© 2023 Rohan Nelson</p></footer>
<style>
  :root {
  }
  footer {
    background: var(--grey-gradient);
    height: 40px;
    margin-left: var(--margin-width);
    border-radius: var(--border-radius) 0 0 var(--border-radius);
    display: flex;
    align-items: center;
    justify-content: flex-end;
    padding-right: var(--margin-width);
    margin-top: 80px;
    margin-bottom: 40px;
  }
  p {
    color: white;
    font-size: var(--font-size-h4);
    justify-self: flex-end;
    font-weight: 700;
  }
  @media (max-width: calc(864rem / 16)) {
    footer {
      width: 100vw;
      border-radius: 0;
      margin-left: 0;
      padding-right: 0;
    }
  }
</style>
